; @cloudflare/next-on-pages@1.13.16 (latest) declares a peer of next <=15.5.2,
; but this project runs Next 16. The build works in practice; this flag keeps npm
; from erroring on that mismatch the way pnpm previously tolerated it.
legacy-peer-deps=true
